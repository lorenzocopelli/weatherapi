/// Minimum number of days of weather forecast.
const int minForecastDays = 1;

/// Maximum number of days of weather forecast.
const int maxForecastDays = 14;

/// HTTP status code for a successful request (OK).
const int httpStatusOk = 200;
